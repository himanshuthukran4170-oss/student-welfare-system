services:
  backend:
    build: ./backend
    container_name: welfare-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: welfare-frontend
    restart: unless-stopped
    ports:
      - "5173:5173"
    depends_on:
      - backend